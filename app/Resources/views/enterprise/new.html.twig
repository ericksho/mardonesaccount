{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nueva Empresa</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <br>
        <input type="submit" class="btn btn-success btn-sm" value="Guardar" />
    {{ form_end(form) }}

    <a href="{{ path('enterprise_index') }}" class="btn btn-primary btn-sm">Back to the list</a>
    
{% endblock %}
