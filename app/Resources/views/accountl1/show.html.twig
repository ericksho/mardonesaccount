{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cuenta Nivel 1</h1>

    <table class="table" >
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ accountL1.name }}</td>
            </tr>
            <tr>
                <th>Código</th>
                <td>{{ accountL1.code }}</td>
            </tr>
            <tr>
                <th>Empresa</th>
                <td>{{ accountL1.enterprise.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('accountl1_index') }}">Cuentas</a>
        </li>
        <li>
            <a href="{{ path('accountl1_edit', { 'id': accountL1.id }) }}">Editar</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Borrar">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
